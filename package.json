{
  "name": "inference-providers",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "validate": "pnpm --filter @inference-providers/build run validate",
    "emit": "pnpm --filter @inference-providers/build run emit"
  }
}
