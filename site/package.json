{
  "name": "site",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/copy-artifacts.mjs && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@inference-providers/schema": "workspace:*",
    "@inference-providers/sdk": "workspace:*",
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.0.0"
  }
}
