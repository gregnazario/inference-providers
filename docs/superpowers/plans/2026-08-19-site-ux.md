# inference-providers — Site UX: Providers Popover, Help Page, Finished Search

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans.

**Goal:** (1) Models index shows provider availability — count chip per model with a zero-JS popover listing the providers; (2) a Help/usage section explaining the site, concepts, chips, artifacts, and SDK; (3) search finished properly: header search form (GET → /search/?q=), programmatic Pagefind results on the search page honoring ?q= and a section filter, noscript fallback.

**Constraints:** English; plain commits, no trailers; branch `feat/site-ux`; zero client-side JS EXCEPT the search page's Pagefind usage (existing sanctioned exception); follow existing design tokens/components; all existing verification greps must keep passing.

## Task 1: Providers column + popover on models index

`site/src/pages/models/index.astro`:
- New "Providers" column: for each model, a zero-JS popover built from `<details class="prov-pop">` containing `<summary>` = count chip (e.g. `4`) and a body listing that model's offering providers as linked pills (`${base}providers/${id}/`), each with its wire_id in muted small text; one per offering (so dual-endpoint providers like openai appear once per endpoint — dedupe by provider but show "+chat / +responses" suffix chips? SIMPLER: list unique providers; if a provider serves the model on >1 endpoint show a small "×2" note).
- Styling in Base.astro global CSS (tokens, radius, shadow like cards): `.prov-pop { position: relative } .prov-pop > div { position: absolute; … card styles; z-index; min-width 14rem }`; make `.table-wrap` allow the popover to escape: change overflow strategy on the models index table wrap to `overflow: visible` (keep border radius via th/td corner clipping) — verify no visual regression in both palettes.
- The accumulator already builds per-model offering data — extend it to provider sets.
- Archive page (`archive.astro`) gains the same column (it duplicates the accumulator — factor a shared helper `site/src/lib/modelRows.ts` exporting the row-building function used by both pages; this also resolves the known duplication).
- Verification greps: `grep -c "prov-pop" site/dist/models/index.html` ≥ 36; open one popover's rendered HTML and confirm provider links present; archive page contains prov-pop too.

## Task 2: Help page

NEW `site/src/pages/help.astro` (nav id "help", link between Verify and Download — adjust nav array in Base.astro):
Sections (concise, scannable; reuse existing chip components for the legend):
1. **What this is** — provenance-tracked registry; models vs offerings vs providers; the /verify/ methodology link.
2. **Reading a provider page** — auth methods (incl. OAuth notes), endpoints + protocols, API surfaces, plans, quirks.
3. **Reading a model page** — chips legend: reasoning styles (budget/effort/toggle/adaptive/always_on/none with one-line meanings), mandatory, round-trips values (signature / thought_signature / encrypted_content / reasoning_content — what each means to send back), returns values, status colors, archived banner explanation + 12-month window + grandfather note.
4. **Using the data** — artifacts at /artifacts/ (catalog/providers/models, per-entity); SDK snippet (loadCatalog → resolveModel → authHeaders → buildReasoningParam) with the budget-4096 example rendered as code; sync `pnpm --filter @inference-providers/sync run report`.
5. **Staying current** — daily sync + drift issue, staleness gates (90/180 days), archive policy.
6. **Troubleshooting/FAQ** — 401s (wrong key type: coding vs platform keys; region-bound keys; Baseten Authorization-only), `budget_tokens < max_tokens` rule, why a model shows "—" prices (subscription surfaces / unknown ≠ free).
Verification: `grep -c "Reading a model page" site/dist/help/index.html` ≥ 1; nav link present on homepage.

## Task 3: Finish search

- **Header**: replace the "Search" nav entry with a GET form: `<form class="nav-search" action="${base}search/" method="get"><input type="search" name="q" placeholder="Search…" aria-label="Search"></form>` (styled into the sticky nav, input type=search, Enter submits). Keep a standalone "Search" page for filters/browsing.
- **Search page rewrite** (`site/src/pages/search.astro`): keep the Pagefind module script; add an inline `<script>` that (a) reads `new URLSearchParams(location.search).get("q")`, (b) imports pagefind from `${base}pagefind/pagefind.js`, (c) runs `pagefind.search(q)` and renders results (result.title trimmed of " — inference-providers" suffix? Pagefind indexes full titles; render as-is + excerpt via `result.data()`), (d) if no q, show a prompt + optionally a default empty-state with popular links (Providers / Models / Verify / Help). Render a clear "No results for X" empty state. `<noscript>` message: search requires JavaScript; browse Providers/Models instead.
- **Section filters**: add `data-pagefind-filter="section"` on `<main>`... filters must be per-page attributes: set `data-pagefind-filter="section"` with value via `data-pagefind-filter="section"` on an element whose text is the section? Pagefind filter syntax: `<body data-pagefind-filter="section:providers">` style — attribute value form `data-pagefind-filter="section:providers"` on the main/page wrapper of provider pages ("providers"), model pages ("models"), help/verify/download ("guides"), archive ("archive"). On the search page, render filter chips (All / Providers / Models / Guides) that append `&section=` and pass into `pagefind.search(q, { filters: { section: [value] } })` (only when not "all"). Chips are plain links preserving q.
- **Index quality**: ensure each page has an h1 (all do); add `data-pagefind-meta="description"` is optional — skip.
- Verification: `grep -o 'action="/inference-providers/search/"' site/dist/index.html | head -1`; search page contains `pagefind.pagefind.js` or `pagefind.js` import + `URLSearchParams`; `grep -c "data-pagefind-filter" site/dist` — filters present on provider/model/guide pages; build log "Indexed N pages" unchanged (~62).

## Commits
"Add providers popover to models tables", "Add help page", "Finish search with header form and filters" (order/merge as sensible).
